# 更新日志

## v1.9.3 - 2026-09-23

### 网易云登录错误分型与协议兼容

- 现场确认网易云在扫码状态 802 后返回 HTTP 200 / 业务码 8821，没有签发任何 Set-Cookie。该码表示需要行为验证码验证，不能当作普通网络抖动或登录成功。
- 对 8821 单独标记 `NETEASE_VERIFY`，停止自动重试、清理待登录状态，并私聊提示去官方页面完成人工安全验证；已有 QQ 和网易云凭据不变。不会自动求解或忽略验证要求。
- 网易云原始 Set-Cookie 改为逐条保留域、路径、过期规则后筛选，修复回放测试发现的跨域同名覆盖和过期令牌误接收；这项加固与本次实机 8821 分开记录。
- 新增 `音乐登录 网易云网页`，按官网使用的 type=1 生成/轮询二维码；原 `音乐登录 网易云` 保留 type=3 兼容行为。每个二维码固定自身模式，避免并发串线；遇 8821 两种模式均停止。
- 添加受控 HTTP/业务码与 Cookie 存在布尔诊断，拒绝接收第三方 JSON 正文拼接的 Cookie。没有记录真实 Cookie、二维码地址或用户资料。
- 完成源码发布审计、统一 Python 版本说明并补充第三方来源说明。当前网易云真实登录仍受官方安全验证限制，未宣称已完成账号授权或会员曲目播放；QQ 已验证登录不受影响。
- 全套 528 项本地测试通过（Windows 跳过 1 项 POSIX 权限断言），包含 19 项新增网易云协议/Cookie/模式回归和官方验证提示的隔离测试。

## v1.9.2 - 2026-09-23

### QQ 扫码确认实机修复

- 通过用户现场扫码确认根因：`check_sig` 的同一 HTTP 302 响应同时下发 `graph.qq.com` 下有效的 `p_skey/p_uin/pt4_token`，以及 `qq.com` 下同名、已过期的空 Cookie。旧处理按名称压成字典，后面的删除项覆盖了有效值，导致 `QQ_CHECK_SIG` 失败。
- 仅对此官方签名接口逐条解析原始 Set-Cookie，放入独立 CookieJar，再按 OAuth 目标的域名和路径筛选；不再跨域合并同名 Cookie，不使用“随便取第一个非空值”的宽松兜底。
- 保留同域删除、Max-Age 优先和过期规则，并处理 Expires 时间戳为零的库兼容边缘。请求依旧不自动跟随重定向，不把 Cookie 带到其他域或公共解析站。
- 新增 15 项真实 aiohttp 本地 HTTP 回放，全套 505 项测试通过（Windows 跳过 1 项 POSIX 权限断言）。
- 用户第二次现场扫码完成授权且凭据校验为 valid；使用该账号对《花呗还了吗》《千千万万 (DJ Yaha版)》按原 MID 取流，官方 CDN 均返回 206、audio/mpeg 和有效音频头。该验证只读取各 1 KiB，不等同完整歌曲/语音频道听音验收。

## v1.9.1 - 2026-09-23

### QQ 扫码确认阶段

- 修复 QQ 确认回调和 OAuth Location 的参数顺序依赖：使用严格 URL 参数解析，支持授权码位于最后一个参数；仍拒绝非官方目标、重复关键参数和异常身份字段。
- QQ 登录分为确认回调、签名校验、OAuth 授权、凭据交换四个阶段。按阶段保留仅内存中的已完成状态，后续临时失败不从头重复执行已完成的授权步骤。
- 临时轮询故障允许在原登录截止时间内连续最多三次尝试；授权后的凭据校验遇到 unknown 也允许短暂重试。明确过期、拒绝或协议错误不无限重试，失败仍保留原账号。
- 增加安全的失败阶段提示和诊断日志：仅固定阶段、受限异常类型、HTTP 状态/业务码；不输出二维码、回调 URL、授权码、Cookie、响应正文或异常原文。
- 本次用户截图发生在确认之后，但旧版把异常统一隐藏且未记录阶段，无法反推其确切根因。以上兼容缺陷已离线复现，真实账号是否恢复仍需重新扫码验收。
- 全套 490 项测试通过（Windows 跳过 1 项 POSIX 权限断言）；新增 14 项 QQ 确认阶段回归和 9 项登录重试/诊断保护测试。

## v1.9.0 - 2026-09-23

### 管理员私聊账号登录

- 新增 QQ 音乐的 QQ／微信扫码登录，以及网易云 App 扫码登录；凭据仅发往平台官方 HTTPS API，不交给公共解析站。
- 添加 `音乐登录`、`音乐账号状态`、`取消音乐登录`、`退出音乐账号` 私聊命令。使用独立 KOOK 数字用户 ID 白名单及明确的机器人实例绑定，拒绝群聊、其他平台、未授权用户和错误机器人。
- 启动及定时检查登录状态；明确失效时尝试一次刷新并私聊提醒。通知跨重启去重，网络错误/522/限流不当作登录失效；不主动向管理员发送二维码。
- 登录流程有超时、发起者绑定、取消与代次校验；新账号检查有效后才替换旧账号。二维码消息在终态尝试删除，凭据使用 Fernet 加密、受限文件权限和原子存储。
- 自有账号取歌接入原队列链路，保持同歌曲 ID 与官方音频 CDN 校验；不接受网易云试听结果。账号取歌失败仍可回退已有公共主备源，不向公共源或音频下载器传递 Cookie。

### 审查发现并修复

- 指定适配器消失或多实例变得不明确时，立即关闭旧登录流程，避免旧二维码仍可提交授权。
- 注销/卸载/换账号后丢弃迟到的旧账号音频结果，异步刷新同样复核凭据代次。
- 清理任务独立追踪并屏蔽二次取消，避免退出时漏掉二维码撤回。
- QQ SDK 的 WEB 默认请求未附完整登录字段、通用过期检测会误判任意错误，现补齐实际鉴权字段并只认可明确失效码。
- 私聊命令会终止后续事件传播，防止进入 LLM 链路；回复改为直接发送，避免 AstrBot 在 `stop_event()` 后丢弃普通 yield 回复。
- 兼容 AstrBot 4.28.1 KOOK 适配器的运行元数据 ID 恒为 `kook` 的行为：账号管理按配置实例 ID 绑定，并复核真实 client 身份，避免配置名与运行 ID 不一致导致账号模块未启动或多机器人串线。

### 验证边界

- 已验证 QQ／微信／网易云官方二维码生成、待扫码状态和取消；均未进行真实账号扫码授权，未据此宣称会员播放已验收。
- 网易云官方同 ID 的公开歌曲取流测试返回完整音频地址，无试听标记。
- 本地全套 467 项测试通过（Windows 跳过 1 项 POSIX 文件权限断言），另有 274 项真实 AstrBot 4.28.1 核心检查通过，覆盖实际命令过滤、私聊发送、KOOK 真实配置 ID 行为和停止事件后的回复送达。网络/传输使用替身的检查不等于实机登录验收。
- 服务器部署时额外修正配置原子替换的所有者保留：私有配置需归属 AstrBot 实际服务用户，而不是 SSH 部署用户，避免收紧为 0600 后插件无法读取。
- 自动化回归覆盖存储损坏、权限、实例隔离、换账号/注销竞态、网络故障、重启通知去重、QR 清理与原音乐功能；超时测试通过事件屏障确定阶段，避免依赖 Windows 上不稳定的 15/20ms 调度假设。

## v1.8.1 - 2026-09-22

### QQ 解析主备链路

- 新增 `qq_vip_resolver_backups`，最多 3 个去重备用源；官方匿名音频不可用时，按主源、备用源的配置优先级获取同一 MID 的音频。
- 主源出现 522、超时或其他解析失败时，可立即转下一源；所有源与两阶段请求共用 15 秒预算，不按源数量叠加等待。只有一个可用源时保留最多两次尝试。
- 新增 `qq_vip_resolver_cooldown_seconds`，默认 60 秒。连续两次网络/服务故障后暂时跳过该源，避免歌单逐首撞击故障站；0 可关闭。歌曲不匹配、空歌曲结果或权限限制不导致整站冷却。
- 同 MID、QQ 官方音频 CDN、官方完整版本标题保持不变；并发请求不修改共享的主源地址。成功来源只记录域名与序号，日志不输出 URL 参数或登录凭据。
- 主源留空禁用整条额外解析链路。未默认填入未经验证的公共备用站；管理员可配置自己的兼容取歌服务。

### 排查与验证

- 服务器当前配置只有 `meting.mikus.ink/api`。只读日志及实时请求确认两首用户样本在服务器收到 HTTP 522，响应 `Server: ESA`；同 MID 在本机却返回正确元数据、QQ CDN 重定向和有效的 1 KiB 音频片段。因此不能把该现象直接归因于会员限制、Cloudflare，或认定整个站所有节点都不可用。
- 其他候选公共源虽返回歌曲信息，取流却为空，未作为已验证备用源启用。
- 新增 20 项主备回归测试，覆盖 522 切源、共享预算、冷却/恢复、不同 MID、危险音频 URL、凭据脱敏、取消与并发；全套 307 项测试通过。
- 此条记录仅表示本地代码和测试完成；服务器本次只读检查，未热重载、未改账号配置、未宣称完成自有账号登录或实机备用源播放。

本文件记录 `astrbot_plugin_kook_music` 的重要功能变化和问题修复。

## v1.8.0 - 2026-09-22

### 首播与切歌优化

- Relay 从 UDP/MPEGTS 转为持续 PCM 管道：歌曲进程解码为 48kHz 双声道 PCM，常驻编码器维持同一条 Opus/RTP 时间线。首次先发送 2 秒静音预热，再读取歌曲开头，不再让首曲承担格式探测或重置曲间时间戳。
- 每 20ms 发送一帧，解码缓存最多约 0.5 秒；解码进程提前结束不代表播完，必须等缓冲实际送完，尾部不足一帧补零保留。切歌丢弃旧帧，退出、被踢及重复取消均会回收子进程。
- 新增 `prefetch_next`，默认开启：每个服务器最多后台预准备下一首普通歌曲，不批量下载整个歌单；B站、单曲循环与随机模式不提前准备。准备完成、进行中和失败结果均由队首接管，不重复下载或重新等待同一次解析超时；失败项在轮到时跳过，可重新点歌再试。
- 移除每首歌固定 1 秒起播等待；Relay 不再固定等待 1 秒检查歌曲进程，而是在取得首帧后开始。Direct 模式仍保留原有重连策略。
- 播放卡请求不再阻塞音频播放循环。迟到/取消的 HTTP 请求会取得消息 ID 后清理过期卡片；倒计时在真正发送前扣除已播放时长。Relay 的断流续播按实际送出的歌曲音频量计算，不把预热或网络卡顿填充静音算入跳转位置。
- 预准备对象与实时队列隔离，保留点歌者身份；插队、清空、连续跳过、失败重试、退出及卸载会取消或回收失效准备任务和迟到文件，保持 v1.7.5 的控制权限限制。

### QQ 解析临时故障

- 修复同一分享链接首次提示会员限制、再次却能播放的误导性分类。配置解析源的网络不通、超时、429/5xx、空响应现在显示临时解析服务故障，不沿用官方匿名接口的会员限制提示。
- 配置解析源的两阶段请求和一次重试共用 15 秒预算；连接失败仅清理该解析站的 DNS 缓存，不修改系统 DNS/IPv6 配置。401/403 提示检查解析源配置，证书错误和非法地址不重试。
- 临时失败后不再继续串行叠加其它解析站；单曲链接已尝试解析后，也不会立刻进入播放入口把同一失败链路再执行一遍。同歌曲 ID、完整版本标题和官方 CDN 校验保持不变。

### 验证

- 全套 287 项测试通过，新增 60 项覆盖 PCM 首尾帧、短音 EOF、缓冲耗尽、连续切换、重复取消、预准备失败及迟到文件、权限保持、慢卡片、真实音频计时和 QQ 有界重试。
- 使用合成音频在 Windows FFmpeg 7.1 和服务器 FFmpeg 6.1.1 的纯本地回环中对比：旧中继的 3 秒首曲仅恢复约 0.97 秒、开头标记完全丢失，并发生 2 次 RTP 时间戳跳变；新中继两曲均恢复约 3.01 秒，开头标记保留，RTP 序号/时间戳无断裂，合成曲间间隔约 50-90ms。
- 回环测试仅使用 `127.0.0.1`，不向 KOOK 频道发送测试音。上述毫秒指标不等同于真实网络歌曲或客户端验收；解析服务不可用、预准备尚未完成或 B站即时解析时仍可能有等待。

## v1.7.5 - 2026-09-22

### 播放控制权限

- 修复卡片按钮未检查点击者权限的遗漏。下一首、循环、清空仅允许当前歌曲的点歌者，或当前 KOOK 服务器所有者/拥有原生管理员权限的成员使用；缺少点击者身份、未知点歌者或权限查询失败时拒绝操作。
- 管理员身份通过 KOOK `guild/view` 和带 `guild_id` 的 `user/view` 实时核验，验证服务器/用户 ID、角色 ID 与管理员权限位；不信任按钮携带的角色快照、角色名称或 AstrBot 全局管理员的裸数字 ID。每次重新查询，不缓存管理员授权。
- 下一首、循环模式、清空歌单、队列插队、退出语音等文字命令共用同一权限入口，避免改用命令绕过按钮限制。查看歌单及正常点歌行为不变。
- 执行前在会话锁内再次检查当前点歌者和会话身份。连续跳过时以尚未被跳过的有效队首判断权限，上一位点歌者不能通过快速连点继续跳过其他人的歌曲；管理员可以跨点歌者连续控制。
- 额外修复连续切歌尚未提交时的插队竞争：此时暂拒插队/清空，避免重排已获授权的待跳过前缀，导致误跳过其他点歌者或删除即将播放的当前歌曲。
- 仅成功执行退出语音后才清理播放卡片，未授权的退出请求不会删除卡片或改变播放状态。

### 验证

- 新增 46 项权限回归测试，覆盖点歌者、管理员、普通成员、伪造角色、默认角色继承、权限撤销、无身份/错误响应/超时、跨机器人/服务器、连续跳过及插队竞争、会话替换、命令绕过和非 KOOK 静默；全套 227 项测试通过。
- 管理员定义依据 [KOOK 官方权限表](https://developer.kookapp.cn/doc/http/guild-role)：管理员为 bit 0（值 1）；仅拥有管理服务器权限或角色名为“管理员”不等于管理员权限。

## v1.7.4 - 2026-09-22

### QQ 音乐版本标题

- 修复 DJ、Live、重制版等版本标注丢失：QQ 官方的 `name` 是基础歌名，完整客户端标题位于 `title`。现在优先非空 `title`，再回退 `name` / `songname`，不会重复拼接副标题或无关说明。
- QQ 搜索结果、单曲详情、歌单正常项及下架占位统一采用完整标题；播放卡标题、内嵌音频标题和队列卡随歌曲元数据同步显示。
- 同 ID 聚合/Meting 音频兜底保留已取得的官方标题，避免补充音频地址时再次丢失版本标注。歌曲 ID、音频 `media_mid`、时长和非 QQ 平台解析行为不变。
- 用户样本的官方 `title` 为《多想留在你身边 (DJ加快版)》，`name` 为《多想留在你身边》，`subtitle` 为空；`songmid=003uoXDl43gsHW`、`media_mid=003DNAjR40Egq2`、时长 434 秒（7:14）。此前播放源仍是该版本，仅显示文字被截成基础歌名。

### 验证

- 新增 12 项测试，覆盖完整标题、空/异常字段回退、旧接口、避免重复副标题、搜索/歌单/播放卡/内嵌音频标题，以及同 ID 音频兜底保持官方版本名；全套 181 项测试通过。

## v1.7.3 - 2026-09-22

### QQ 音乐分享单曲修复

- 修复 `点歌` 无法解析 `c6.y.qq.com/base/fcgi-bin/u` 官方单曲分享短链的问题。此前只有歌单导入展开短链，单曲入口仅从原始 URL 读取 `songmid`，导致数字歌曲 ID 的分享链接直接失败。
- 支持 `songid` 数字参数、手机分享页面及 `ryqq_v2/songDetail` 新版路径；数字 ID 使用官方详情接口的 `song_id` 参数查询，再按返回的规范 `songmid` 解析播放地址，不会按同名歌曲替换版本。
- 链接提取兼容 KOOK Markdown、加粗/反引号、HTML 实体和转义下划线；优先读取 Markdown 目标地址，不把 `](...)` 拼进分享参数。歌单入口同步使用此规范化逻辑。
- 分享短链逐跳验证 QQ 官方 HTTPS 域名、端口及身份信息，拒绝跨域、协议降级、异常地址和重定向循环；最多跟随 6 跳，总等待不超过 20 秒。原有官方 HTTP 直链安全升级为 HTTPS，保留兼容性。

### 验证

- 新增 19 项回归测试，覆盖短链、数字 ID、手机/新版路径、KOOK 命令到播放入口、格式化歌单链接、HTTP 兼容、恶意跳转、异常响应及超时；全套 169 项测试通过。
- 用户提供的分享链接已通过修复后的完整解析函数取得歌曲 `002BIs840y3O8c`，歌名《两个人的回忆一个人过》、歌手季彦霖、时长 189 秒；官方音频 CDN 的 Range 请求返回 HTTP 206 / `audio/mpeg`，前 4096 字节含有效 ID3 标识。未将此检查等同于 KOOK 语音频道实际播放验收。

## v1.7.2 - 2026-09-12

### 卡片按钮修复

- 修复冷启动时插件先于 KOOK 适配器初始化、按钮事件监听只安装一次而永久漏装的问题。现在每 2 秒幂等同步，并在处理 KOOK 命令前立即补装，覆盖平台晚启动、客户端重建与后台新增实例。
- 监听所有 KOOK 客户端的实际 `event_callback`；非 KOOK 平台不注入、不回复。卸载时恢复原回调；即使其他插件保留了旧包装函数，旧函数也只转发事件，不再重复处理按钮。
- 下一首、清空与循环按钮兼容对象、字典和网关封套事件；按卡片消息/文字频道定位当前会话，并校验来源 Token，避免误操作其他机器人会话。按钮缺少文字频道时只接受已知卡片映射，不把系统事件外层 `target_id` 当作文字频道。
- 手填 `kook_token` 与所有 KOOK 适配器均不一致时给出明确诊断，不输出 Token。按钮操作失败不会终止消费循环；连续点击逐个执行，清空仍保留正在播放的歌曲，仅移除待播队列。

### 被移出语音频道

- 机器人收到明确的语音 WebSocket `disconnect` 通知，或主网关中匹配机器人自身、来源 Token 和语音频道的 `exited_channel` 事件时，立即结束本次播放：取消音频准备、播放和重试任务，停止 Direct/Relay 推流，清理队列及会话，并触发播放卡清理。
- 明确被移除的客户端不会自动重连或继续下一首；再次点歌会建立新的会话。其他成员退频道、无关频道退出、正常 Direct 切歌重连和普通网络 EOF 不按踢出处理。
- 修复清理任务可能等待自身、旧播放循环可能影响替换会话的问题；退出清理独立于按钮回复，不阻塞主网关事件分派。
- 播放卡发送与删除按服务器串行：被踢出时若卡片仍在发送，响应返回后也会被清理，不再留下迟到的“正在播放”卡；旧歌曲回调及旧结束回调不能覆盖/删除新会话的播放卡。
- 回归审查额外发现并修复：加入语音/启动 Relay 尚未完成时，主网关退出事件因会话未发布而漏处理。初始化中的会话现在也可被定位并取消，不会在被踢后才开始播放。
- 正常切歌退出的去重与先后判定使用 KOOK `joined_at` / `exited_at` 服务端时间，避免本机时钟偏差将真实踢出误认为旧事件；迟到的加入事件不会回退时间基准。
- 卸载时进入关闭状态，同时拒绝仍在等待全局创建锁的其他服务器点歌请求，避免清理结束后又创建新的语音连接。

### 验证

- 新增冷启动晚绑定、热重载、多适配器、事件形状、快速连续切歌、清空保留当前歌曲、网关按钮驱动真实队列管理器、Direct/Relay 踢出停止、准备中/空闲中/等待重试中退出、重复退出和旧客户端事件等离线回归测试。
- 共 150 项自动化测试通过（本次新增 44 项）；新增测试同时通过 Python 开发模式检查，`compileall`、配置 JSON 解析和 `git diff --check` 通过。
- 本次未连接生产 KOOK 机器人；自动化模拟与静态检查不代替部署后的真实语音及卡片点击验收。

## v1.7.1 - 2026-07-23

### 长视频播放

- 新增 `bili_stream_threshold_minutes`，默认 30 分钟。达到阈值的单个视频/分P以及未知时长内容在轮到队首时 fresh 解析 B站音频 CDN 地址并直接交给 FFmpeg，不再等待完整文件下载；设为 `0` 可恢复全量下载。
- 临时签名流地址与 BVID/分P元数据分离，不会在导入、卡片或日志中暴露。排队、连续切歌、清空、退出和循环重播都会丢弃旧签名，下一次实际播放前重新解析；解析/下载期间切歌会取消当前准备任务，不再等待大文件落盘。
- Direct 与 Relay 两种模式均支持安全 HTTPS 输入；FFmpeg 携带 yt-dlp 返回的 `User-Agent` 与 `Referer`，启用有限网络重连和 15 秒读超时。B站 Cookie 只用于 yt-dlp 鉴权，不会传入 FFmpeg 命令行或 CDN 请求头。
- 数小时流若中途异常退出，会丢弃旧签名、fresh 解析一次，并使用 FFmpeg 输入 seek 从已播放位置前约 5 秒续播；Direct 模式同时刷新 RTP，Relay 模式复用中继。第二次失败才继续后续队列，避免无限重试。
- 未知时长歌曲不再套用 600 秒固定上限，改为等待 FFmpeg 自然 EOF；已知数小时内容仍按真实时长加 30 秒保护。Relay 现在会保留歌曲进程退出码，能区分正常结束与网络/转码失败。

### 卡片与多P修复

- B站播放卡的来源、点歌人、队列状态改为独立 context 行，避免 KOOK 将多个元素横向挤压；超过一小时的时长改为 `H:MM:SS`。
- 解析进度消息不再回显用户粘贴的完整视频 URL，避免 KOOK 或其他链接预览机器人被二次触发。
- 针对 `BV1ANQqBTEVU` 回归确认：该链接是总长 `6:17:29` 的 100P 合集，当前版本展开为 100 个 FIFO 队列项；P1 为 4:57，UP主、分P标题、时长和封面均能正确取得。旧运行实例的总标题、未知UP主、无时长和只播放 P1 现象不再沿用。
- FFmpeg 启动失败及后台 stderr 统一脱敏 HTTPS 地址，防止错误文本回显 B站 CDN 签名参数。

### 验证

- 通过 106 项仓库自动化测试，覆盖 100P 展开、长/未知时长流式选择、短视频下载回退、Direct/Relay 参数与日志脱敏、异常断流 fresh 解析与断点续播、未知/3 小时时限、循环刷新签名、并发入队清签名、连续切歌、小时格式、卡片布局和解析提示。
- 联网实测 P1 fresh 解析得到 HTTPS 音频流，仅向 FFmpeg 传递 `Referer`/`User-Agent` 后连续实时读取 3 秒并正常退出；元数据为 UP主“那首你最爱的歌谣啊”、时长 297 秒且封面为 HTTPS。

## v1.7.0 - 2026-07-23

### 新增

- B站命令由 `播放b站` 简化为可扩展的 `播放`，支持关键词、BV号、视频链接、分P链接、收藏夹链接和 `b23.tv` 官方短链。
- 未显式指定 `?p=N` 的多P视频会按原始分P顺序整批加入队列；显式分P只播放对应一P。收藏夹中的多P视频默认只加入 P1，避免递归展开。
- 支持空间收藏夹、`medialist` 与 `list/ml...` 链接；私密收藏夹可复用 `bili_cookie` 中的 SESSDATA 鉴权。
- 大型收藏夹和多P视频复用歌单区间选择、超时取消、实时容量复检与整批原子入队。收藏夹先建立轻量占位，只请求最终选中区间覆盖的分页。

### 修复与安全

- B站元数据提取与音频下载统一启用 `noplaylist`，并在下载时从已验证的 BVID 与页码重建官方 URL，避免 yt-dlp 意外展开整套分P或播放错误分P。
- 修复多P歌曲卡片把内部 `_pN` ID 拼进链接导致跳转无效的问题；播放卡现在始终指向精确的 `?p=N` 页面。
- B站 API 增加全局串行节流、412/429/5xx 与 `-412/-509/-352` 风控码退避重试、收藏夹同页请求合并和短时缓存，降低大型收藏夹触发风控的概率。
- 关键词播放优先解析 B站服务端渲染搜索页，并保留官方搜索 API 与 yt-dlp 兜底，避免 `bilisearch1` 单一路径遇到 HTTP 412 后完全不可用。
- 修复非法 `p=`、`p=abc`、`p=0` 被误当成“未指定分P”并展开全集的问题；官方短链采用限制跳转次数与域名白名单的手动展开。
- Cookies 文件只读取精确 B站域名下的必要 Cookie，拒绝 `evilbilibili.com` 等相似域名；收藏夹失效、删除或非视频项目会单项跳过而不中断整个批次。
- 修复慢请求可能晚到并反向替换较新请求、以及请求在语音频道查询期间被替换后仍然入队的竞态；最终 marker 校验与原子入队现在使用同一提交锁线性化。
- 视频详情的成功与失败短时缓存都限制为最多 256 项，避免随机无效 BV 输入让负缓存持续增长。

### 队列与下载行为

- 收藏夹/多P批次通过 `join_and_play_many` 追加到已有点歌之后，保持 FIFO；容量不足时整批拒绝，不会产生部分入队。
- 批量添加阶段仅保存视频元数据，不会预下载全部音频；每一项轮到队首后才由 yt-dlp 下载，连续切歌仍沿用现有播放任务与跳过计数逻辑。

### 验证

- 自动化测试覆盖 B站链接与伪域名、非法分P、官方短链、多P顺序、显式分P、收藏夹占位与按页物化、同页请求合并、私密收藏夹、失效条目跳过、Cookie 域名边界、`noplaylist` 和精确播放卡链接。
- 通过 83 项仓库自动化测试、`compileall`、配置 JSON 校验和差异检查；新增覆盖请求替换期间禁止旧批次提交、负缓存上限和服务端渲染搜索结果解析。
- 联网冒烟测试确认关键词“海阔天空”解析为 `BV1WKg3z5Ehi`，`BV1dujdzrEA4` 可按顺序展开 5 个分P，显式 P2 只下载对应的 `563,238` 字节 M4A；公开收藏夹 `213003412` 读取到 7 项并仅物化选中的前三项，`https://b23.tv/BV1dujdzrEA4` 可安全展开到官方长链接。

## v1.6.0 - 2026-07-19

### 新增

- QQ 音乐官方匿名 vkey 返回会员拒绝后，可按原始 `songmid` 调用可配置的会员解析源；默认使用 `https://meting.mikus.ink/api`，配置留空即可禁用。
- 兼容 Meting API 与 `api/song/url` 响应格式，便于切换到自建解析服务；后者可配置 `128`、`320` 或 `flac` 请求音质。
- 配置会员解析源后，QQ 搜索会保留付费歌曲候选；未配置时继续只显示匿名可完整播放的歌曲。

### 安全与兼容

- 免费歌曲始终优先走 QQ 官方播放链路，会员解析仅作为官方拒绝或临时失败后的同 ID 兜底。
- Meting 解析先校验显式 `songmid` 或 URL/LRC 中的歌曲 ID，再禁止自动跟随重定向；其他解析响应同样必须精确包含请求 MID，禁止按同名歌曲替换。
- 解析成功后保留 QQ 官方标题、歌手、时长和封面，并清除旧的会员拒绝原因；解析源失败时仍保留官方拒绝结果，临时故障才继续现有同 ID 备用链路。
- 支持把自建解析 API 配置为本机或内网地址，但最终音频必须跳转到 QQ 官方 HTTPS 音频 CDN；同时拒绝十进制整数、缩写和八/十六进制形式的私网 IP 地址。

### 联网验证

- QQ 会员曲 `000WdUWo1p4MAt`（Russ《Psycho,Pt. 2》）经默认解析源取得 QQ CDN 的完整 M4A：`1,988,937` 字节，`ffprobe` 时长 `162.052` 秒；不是官方匿名链返回的 60 秒试听片段。
- 免费对照曲 `001iLFTh0fcAdb` 仍由 QQ 官方匿名 vkey 直接解析，没有经过会员解析源。
- 禁用会员解析源后，会员曲仍按原逻辑返回 `resolver_status=denied`，不会误播试听或同名版本。
- 通过 63 项自动化测试，并通过 `compileall`、配置 JSON 校验和差异检查。

### 已知限制

- 默认公共解析服务依赖服务方的 QQ 音乐会员 Cookie，没有可用性保证；长期部署建议自建 `mikus-loli/Meting-API` 并配置自己的有效会员 Cookie。
- 默认 Meting 源当前为完整 C400 AAC（约 96 kbps），不是无损音质；`qq_vip_resolver_quality` 仅对支持该参数的 `api/song/url` 类解析源生效。

## v1.5.1 - 2026-07-19

### 修复

- 支持 `c6.y.qq.com/base/fcgi-bin/u?...` 格式的 QQ 音乐官方分享短链接。插件会在导入前展开受控重定向，再从官方分享页提取歌单 ID。
- 短链接解析仅允许在 `y.qq.com` 及其子域之间跳转，并限制重定向次数与请求超时；跨域或指向本地地址的跳转会被拒绝。

### 验证

- 用户提供的 QQ 短链接成功解析为歌单 `1009789507` 并完整导入 1093 首；导入后没有预下载文件，实际轮到的免费歌曲可以完成 MP3 下载。
- 用户提供的网易云链接成功解析为歌单 `14301996301` 并完整导入 316 首；`1-200`、`201-316` 和 `117-316` 区间均正确，超过 200 首或越界区间会拒绝。
- 通过 53 项自动化测试，新增 QQ 官方短链接解析与跨域重定向拒绝覆盖。

## v1.5.0 - 2026-07-19

### 新增

- `导入歌单` 扩展到 QQ 音乐与酷狗音乐。QQ/酷狗官方歌单链接可自动识别；数字 ID 可使用 `导入歌单 qq <ID>`、`导入歌单 酷狗 <ID>`，裸数字继续默认网易云以保持兼容。
- 支持新版酷狗 `gcid_...`、旧版数字 `specialid` 以及 `collection_...` 歌单 ID，包括 `specialid=0` 的用户自建歌单。
- QQ/酷狗歌单沿用大歌单区间选择、超时取消、实时剩余队列容量检查和整批原子入队；最大队列配置仍支持 2000 首。
- 歌单导入结果卡片显示来源平台；三平台歌曲可以与单曲点歌按 FIFO 顺序混合播放。

### 修复与兼容

- QQ 歌单优先使用可返回完整原始顺序的官方接口，并严格校验返回总数；分页兜底按固定页长推进，平台过滤或无效项保留不可播放占位，避免用户选择的歌单序号偏移。
- 酷狗歌单优先读取 `nofilt` 完整列表并校验分页总数、更新时间和原始顺序；签名接口失败时再使用 `song_v2` 过滤列表兜底。
- QQ/酷狗歌曲只在实际轮到播放时刷新临时播放地址，不会在导入 200/1000 首歌单时提前下载全部音频；会员、付费、版权或下架条目会被跳过并继续后续队列。
- 仅网易云歌单执行网易云元数据补全，避免将 QQ `songmid` 或酷狗 `FileHash` 错发给网易云详情接口。
- 修复 QQ 超过 1000 秒的长音频时长被误当成毫秒的问题，并保留旧版无协议 `playlist?id=...` / `djradio?id=...` 输入兼容。
- 歌单链接解析严格校验官方域名，避免相似恶意域名被当作 QQ/酷狗链接。

### 验证

- 通过 51 项自动化测试，新增覆盖三平台歌单链接解析、QQ 完整列表与分页回退、酷狗 Android/Web 固定签名向量、`gcid` 解码、数字 ID 转换、完整列表分页、不可播放占位和平台结果卡片。
- 联网验证 QQ 1345 首歌单、酷狗 100+ 首普通歌单、`specialid=0` 用户歌单和 952 首大型歌单均能保持顺序导入；QQ 免费曲目可懒解析播放，会员曲目会明确拒绝而不中断队列。

## v1.4.0 - 2026-07-19

### 新增

- QQ 音乐搜索、歌曲详情和匿名可播放地址解析。使用 QQ 音乐网页接口获取稳定 songmid，并在播放前按 media_mid 刷新临时 vkey。
- 酷狗音乐搜索和匿名可播放地址解析。使用酷狗网页搜索及播放接口，以 FileHash 保持歌曲身份并在播放前刷新临时地址。
- 支持常见 QQ 音乐单曲链接、酷狗带 Hash 链接以及新版酷狗单曲网页链接直接点歌。
- 搜索结果和正在播放卡片显示中文音乐平台名称。

### 修复

- 修复第三方聚合接口从 `songs` 改为 `data` 后 QQ 音乐搜索始终为空的问题。
- 修复原聚合接口酷狗搜索返回 404，导致配置中虽然存在酷狗但实际无法点歌的问题。
- 修复将 QQ 的会员权益字段以及酷狗的 Privilege/PayType 直接当作不可播放标志，误删实际可播放歌曲的问题；现在以平台播放接口返回的完整 URL 为准。
- 修复平台解析暂时失败时无法进入同歌曲 ID 备用解析源，以及备用接口返回其他 ID 时可能播错歌曲的问题。
- 修复旧式 QQ 单曲链接可能被网易云正则截断并误识别为网易歌曲的问题。
- 修复 QQ 临时 M4A 地址仍按 `.mp3` 后缀保存的问题；下载器现在按响应类型保存 MP3/M4A/AAC/OGG/FLAC/WAV。
- 修复解析站返回空 HTML、图片或伪装二进制内容时仍被保存并交给 FFmpeg 的问题。
- 搜索选歌等待状态按用户、服务器和文字频道隔离；同频道的新搜索会安全替换旧搜索，不再覆盖 Future 或误删新卡片。

### 限制与验证

- QQ/酷狗仅播放平台允许匿名完整播放的资源。会员、付费、版权或地区受限歌曲会给出明确原因，不会把几十秒试听片段伪装为完整歌曲。
- 本版本扩展的是单曲搜索、直接链接和混合队列播放；`导入歌单` 目前仍为网易云歌单/电台功能。
- 通过 37 项自动化测试，覆盖平台字段兼容、稳定 ID 刷新、付费拒绝、错误 ID 防护、M4A 下载、无效响应拒绝和网易云/QQ/酷狗混合播放流。
- 联网冒烟测试实际完成 QQ MP3（4,190,664 字节）与酷狗 MP3（3,047,954 字节）下载，并验证两平台常见单曲链接解析成功。

## v1.3.1 - 2026-07-19

### 新增

- 支持超大网易云歌单按歌曲序号区间导入。选择区间必须小于等于当前队列剩余容量，超时后自动结束本次导入。
- 歌单最大队列配置上限由 200 提升至 2000，并增加歌单区间选择等待时间配置。
- 歌单批量入队改为原子操作：容量足够时整批追加，容量不足时整批拒绝，不会产生只加入部分歌曲的队列。

### 测试中发现并修复

- 修复等待歌单区间时拦截普通聊天或其他命令，以及与搜索结果数字选择冲突的问题。
- 修复同一用户在不同频道导入歌单时可能共享等待状态，以及连续发起导入时旧请求覆盖新请求的问题。
- 修复极长数字区间输入可能触发整数转换异常的问题。
- 修复大歌单导入时过早获取全部歌曲时长造成额外等待的问题；现在仅补全最终选中区间的歌曲信息。
- 修复第三方歌单接口返回不完整时歌曲缺失或顺序不准确的问题；使用网易云官方歌曲 ID 顺序校验并补齐结果。
- 修复大队列卡片可能超过 KOOK 卡片模块数量限制的问题。
- 修复单曲循环模式下点击跳过仍重复当前歌曲的问题。
- 修复短时间连续点击多次跳过可能只生效一次的问题；每次跳过现在对应消费一首歌曲。
- 修复下载失败与跳过同时发生时可能额外丢弃下一首歌曲的问题。
- 修复 Direct 模式在跳过或启动播放失败后没有及时刷新 RTP 连接，导致后续歌曲无法正常推流的问题。
- 修复首次并发点歌可能重复创建语音会话，以及加入语音过程中退出时残留会话的问题。
- 修复清空队列、取消下载或 B 站后台下载被取消时可能残留临时音频文件的问题。
- 修复超长队列中的旧播放链接失效后无法恢复的问题；下载失败时会重新解析歌曲链接并重试一次。
- 修复语音连接连续恢复失败后队列仍保持非空、播放任务卡死的问题。
- 修复运行时队列上限和等待时间配置缺少类型与范围保护的问题。

### 验证

- 通过 19 项仓库自动化测试，覆盖单曲与歌单混播、FIFO 排序、连续跳过、下载失败、清空队列、并发加入/退出和语音恢复失败等场景。
- 完成 Direct/Relay 播放路径以及关闭循环、单曲循环、列表循环和随机播放四种模式的动态模拟。
- 使用 1200 首模拟歌单验证歌曲顺序与缺失信息补齐，并使用 2000 首队列验证 KOOK 卡片模块数量限制。
- 用户示例网易云链接在验证时读取到 316 首歌曲，区间解析、信息补全和选中歌曲顺序均正常。

## v1.2.4 - 2026-07-19

- 首次公开版本。
